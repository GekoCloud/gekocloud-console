"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _react = _interopRequireDefault(require("react"));

var _reactTestRenderer = _interopRequireDefault(require("react-test-renderer"));

var _Button = _interopRequireDefault(require("./Button"));

it('renders correctly', function () {
  var tree = _reactTestRenderer.default.create(_react.default.createElement(_Button.default // one of [primary, default, danger, text]
  , {
    type: "primary",
    className: "btn-create-something",
    style: {
      marginRight: '20px'
    },
    loading: false,
    disabled: false,
    size: "default",
    onClick: function onClick() {
      return console.log('btn clicked');
    },
    htmlType: "submit",
    children: "btn inner text"
  }), {
    createNodeMock: function createNodeMock(element) {
      return {
        current: {}
      };
    }
  }).toJSON();

  expect(tree).toMatchSnapshot();
});
it('renders jump link', function () {
  var tree = _reactTestRenderer.default.create(_react.default.createElement(_Button.default, {
    href: "https://example.com",
    target: "_blank" // one of [primary, default, danger, text]
    ,
    type: "primary",
    className: "btn-create-something",
    style: {
      marginRight: '20px'
    },
    loading: false,
    disabled: false,
    size: "default",
    children: "btn inner text"
  })).toJSON();

  expect(tree).toMatchSnapshot();
});