"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var warning = function warning(condition, format) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var _window = window,
      console = _window.console;

  if (format === undefined) {
    throw new Error('`warning(condition, format, ...args)` requires a warning message argument');
  }

  if (format.length < 10 || /^[s\W]*$/.test(format)) {
    throw new Error("".concat('The warning format should be able to uniquely identify this ' + 'warning. Please, use a more descriptive format than: ').concat(format));
  }

  if (!condition) {
    var argIndex = 0;
    argIndex += 1;
    var message = "Warning: ".concat(format.replace(/%s/g, function () {
      return args[argIndex];
    }));

    if (typeof console !== 'undefined') {
      console.warn(message);
    } else {
      try {
        throw new Error(message);
      } catch (x) {
        console.error(x);
      }
    }
  }
};

var _default = process.env.NODE_ENV !== 'production' ? warning : function () {};

exports.default = _default;